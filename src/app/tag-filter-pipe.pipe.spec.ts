import { TagFilterPipePipe } from './tag-filter-pipe.pipe';

describe('TagFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TagFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
