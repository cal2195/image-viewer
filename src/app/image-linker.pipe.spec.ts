import { ImageLinkerPipe } from './image-linker.pipe';

describe('ImageLinkerPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageLinkerPipe();
    expect(pipe).toBeTruthy();
  });
});
